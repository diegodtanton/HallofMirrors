<< GCP >>

Spin up / unpause VM

<< LOCAL >>

Close any existing VM windows and terminals
Update shared/config.py file

# Open Terminal 1
python -m utils.gcp.connect

<< VM >>

sudo pkill -f python
htop
cd ..
sudo rm -rf gauges_experiment
Close any existing VM windows and terminals

<< LOCAL >>

# Open Terminal 1
python -m utils.gcp.push_code
python -m utils.gcp.view

# Open Terminal 2
python -m utils.gcp.view

# Open Terminal 3
python -m utils.gcp.connect

<< VM >>

cd gauges_experiment
ls
./launch_parallel.sh
htop

<< LOCAL >>

python -m utils.gcp.pull_data

# Change shared/config.py file to new downloaded_data path

python -m mirrors.gauges
python -m utils.plot_story